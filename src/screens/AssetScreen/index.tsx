import { ThemedText } from "@/src/components/ThemedText";
import { FC } from "react";
import { ScreenContainer } from "react-native-screens";

export const AssetScreen: FC = () => (
  <ScreenContainer>
    <ThemedText>
      Asset screen
    </ThemedText>
  </ScreenContainer>
)